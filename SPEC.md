# SPEC.md — Customer Service Dashboard

## 1. מטרת הפרויקט

מטלת לימודים לבניית דאשבורד שירות לקוחות מקצועי, ברור ונוח לשימוש. הדאשבורד מציג תמונת מצב של פניות הלקוחות ומאפשר להבין:

- את מצב השירות הנוכחי
- עומסי עבודה
- זמני טיפול בפניות
- שביעות רצון לקוחות

## 2. משתמשים

בשלב זה — משתמש יחיד, ללא מנגנון התחברות או הרשאות. עם זאת, הקוד ייבנה עם הפרדה ברורה בין שכבת הנתונים לשכבת התצוגה, כך שבעתיד יהיה ניתן להוסיף תפקידים (למשל נציג מול מנהל) מבלי לשנות את המבנה הבסיסי.

## 3. נתונים

### 3.1 מקור נתונים

בשלב הראשון עובדים אך ורק עם **נתוני דוגמה (Mock Data)**, שנשמרים כקובץ נתונים סטטי בפרויקט.

הגישה לנתונים מתבצעת דרך שכבת שירות (`services`) עם פונקציות כגון `getTickets()`, כך שבעתיד ניתן יהיה להחליף את המימוש הפנימי (למשל למעבר ל-API אמיתי) מבלי לשנות את שאר האפליקציה.

### 3.2 מבנה נתונים — Ticket

| שדה | תיאור |
|---|---|
| `id` | מזהה ייחודי לפנייה |
| `subject` | כותרת/נושא הפנייה |
| `category` | קטגוריה: בעיות תשלום / תמיכה טכנית / החזרים / אחר |
| `status` | סטטוס: פתוח / בטיפול / סגור |
| `priority` | עדיפות: נמוך / בינוני / גבוה |
| `customerName` | שם הלקוח |
| `assignedAgent` | הנציג המטפל בפנייה |
| `createdAt` | תאריך ושעת פתיחת הפנייה |
| `firstResponseAt` | תאריך ושעת התגובה הראשונה (אם קיימת) |
| `resolvedAt` | תאריך ושעת סגירת הפנייה (אם קיימת) |
| `csatScore` | ציון שביעות רצון 1–5 (אופציונלי, לא לכל פנייה) |

כל מדדי הזמן (זמן תגובה, זמן פתרון) מחושבים דינמית מתוך השדות הבסיסיים (`createdAt`, `firstResponseAt`, `resolvedAt`) ולא נשמרים כשדות נפרדים — כדי למנוע כפילות ואי-התאמות כאשר בעתיד יתחברו נתונים אמיתיים.

## 4. מדדים מרכזיים (KPIs)

1. **נפח פניות**
   - מספר פניות פתוחות כרגע
   - מספר פניות שנסגרו (בטווח הנבחר)
   - מספר פניות שהגיעו היום

2. **זמני תגובה וטיפול**
   - זמן תגובה ממוצע (`firstResponseAt - createdAt`)
   - זמן פתרון ממוצע (`resolvedAt - createdAt`)

3. **חלוקה לפי נושאים**
   - התפלגות פניות לפי `category`

4. **שביעות רצון לקוחות (CSAT)**
   - ציון CSAT ממוצע מתוך הפניות שדורגו

## 5. רכיבי הדאשבורד

- **כרטיסי KPI** — תצוגה ברורה של המדדים המרכזיים בראש הדאשבורד
- **גרפים וויזואליזציות**:
  - מגמת נפח פניות לאורך זמן
  - התפלגות פניות לפי קטגוריה
  - התפלגות פניות לפי סטטוס
  - מגמת CSAT
- **טבלת פניות מפורטת** — הצגת רשימת הפניות עם כל השדות הרלוונטיים
- **חיפוש** — חיפוש חופשי לפי נושא הפנייה / שם לקוח
- **סינון** — לפי סטטוס, נושא (קטגוריה), טווח תאריכים ונציג
- **עיצוב** — מקצועי, ברור, רספונסיבי ונוח לקריאה

## 6. סטאק טכנולוגי

- **React** + **TypeScript** — בניית הממשק
- **Vite** — כלי בנייה והרצה
- **Tailwind CSS** — עיצוב
- **Recharts** — גרפים וויזואליזציות

ללא backend או מסד נתונים בשלב זה — כל הלוגיקה רצה בצד הלקוח (client-side) מול נתוני הדוגמה.

## 7. ארכיטקטורה

```
src/
  data/         נתוני הדוגמה (Mock Data)
  services/     שכבת גישה לנתונים (getTickets וכו') — נקודת החיבור העתידית ל-API אמיתי
  types/        טיפוסי TypeScript (Ticket וכו')
  utils/        פונקציות חישוב (KPIs, זמני תגובה/פתרון)
  components/   רכיבי UI (KPI Cards, Charts, Table, Filters, Search)
  App.tsx       הרכבת הדאשבורד
```

עקרון מנחה: שכבת ה-`services` היא נקודת ההפרדה היחידה בין הדאשבורד לבין מקור הנתונים. מעבר לנתונים אמיתיים בעתיד ידרוש שינוי בקובץ השירות בלבד, ללא נגיעה ברכיבי התצוגה.

## 8. מחוץ לסקופ (שלב 1)

- אימות והרשאות משתמשים
- חיבור ל-Backend / מסד נתונים אמיתי
- עדכון נתונים בזמן אמת
- שליחת הודעות/התראות ללקוחות או לנציגים
